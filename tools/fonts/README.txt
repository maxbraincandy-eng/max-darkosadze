tools/fonts/ holds the two Noto Georgian variable fonts (SIL Open Font License),
used only by the scripts in tools/ to draw the share images and brand assets.
The website itself loads the same families from Google Fonts.
